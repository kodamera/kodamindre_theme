{% if context.list_style %}
  {% set list_attributes = attributes.addClass('list-style-none') %}
  {% set list_item_attributes = attributes.addClass('block') %}
{% endif %}
{%- if items or empty -%}
  {%- if title is not empty -%}
    <h3>{{ title }}</h3>
  {%- endif -%}
  {%- if items -%}
    <{{ list_type }}{{ list_attributes }}>
    {%- for item in items -%}
      <li {{ item.list_item_attributes }}>{{ item.value }}</li>
    {%- endfor -%}
    </{{ list_type }}>
  {%- else -%}
    {{- empty -}}
  {%- endif -%}
{%- endif %}
