{%
  set classes = [
    'flex',
    'flex-col',
    'w-full',
    'c-region',
    'c-region--' ~ region|clean_class,
  ]
%}
{% if content %}
  <div{{ attributes.addClass(classes) }}>
    {{ content }}
  </div>
{% endif %}
