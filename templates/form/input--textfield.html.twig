{#
/**
 * @file
 * Theme override for an 'input' of type textfield form element.
 *
 * @see template_preprocess_input()
 */
#}
{%
  set classes = [
    'c-form-item__input',
    'c-form-item__input--text'
  ]
%}
<input{{ attributes.addClass(classes) }} />{{ children }}
