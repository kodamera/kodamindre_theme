{%
  set classes = [
    'c-button',
    'c-button--primary',
    'max-w-full',
    'first:mr-4',
    'bg-gray-200 text-gray-900 inline-block cursor-pointer px-6 py-4'
  ]
%}
<input{{ attributes.addClass(classes).removeClass('button', 'button--primary') }} />{{ children }}
