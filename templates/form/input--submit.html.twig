{%
  set classes = [
    'max-w-full',
    'first:mr-4',
    'bg-gray-200 text-gray-900 inline-block cursor-pointer px-6 py-3'
  ]
%}
<input{{ attributes.addClass(classes).removeClass('button', 'button--primary') }} />{{ children }}
